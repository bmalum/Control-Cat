#!/usr/bin/env ruby

require 'control_cat'

chatter = ControlCat::Chatter.new
config = ControlCat::ConfigLoader.new
start = ControlCat::Starter.new


chatter.say_hello
config.first_start
chatter.say_adios
